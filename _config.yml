title: ProblemRider
description: A comprehensive catalog of software development problems, their symptoms, their root causes, and the solutions that address them
baseurl: ""
url: "https://feststelltaste.github.io/problemrider"
lang: en

# Build settings
markdown: kramdown
highlighter: rouge
theme: minima

# Collections
# problems_de/solutions_de hold the German translation (see
# plans/german-translation-plan.md). They use their own German-derived
# filenames/slugs rather than mirroring the English ones, so each entry
# carries an `en_slug` front-matter field pointing back to its English
# original for cross-linking and the language switcher.
collections:
  problems:
    output: true
    permalink: /:collection/:name.html
  solutions:
    output: true
    permalink: /:collection/:name.html
  problems_de:
    output: true
    permalink: /de/problems/:name.html
  solutions_de:
    output: true
    permalink: /de/solutions/:name.html

# Defaults
defaults:
  - scope:
      path: ""
      type: "problems"
    values:
      layout: "problem"
      lang: "en"
  - scope:
      path: ""
      type: "solutions"
    values:
      layout: "solution"
      lang: "en"
  - scope:
      path: ""
      type: "problems_de"
    values:
      layout: "problem"
      lang: "de"
  - scope:
      path: ""
      type: "solutions_de"
    values:
      layout: "solution"
      lang: "de"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

# Plugins
plugins:
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-relative-links

# Plugin configuration
relative_links:
  enabled: true
  collections: true

# Navigation
navigation:
  - title: Home
    url: /
  - title: Problems
    url: /problems/
  - title: Solutions
    url: /solutions/
  - title: Categories
    url: /categories/

# Exclude files
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
  # Agent and contributor instructions are not site content. AGENTS.md is a
  # symlink to CLAUDE.md, and copying it breaks the build when _site already
  # holds a stale copy.
  - AGENTS.md
  - CLAUDE.md
  - GEMINI.md
  - MISTAKES.md
  - problem-pattern-template.md
  - solution-pattern-template.md
  - quality-tactics-reference.md
  - SOLUTIONS_PLAN.md
  - neo4j_simple/
  - scripts/*
  - CLAUDE.md
  - GEMINI.md
  - README.md
  - git-tail.sh
  - embeddings/
  - plans/
  - TECHDEBT.md
  - ARCH.md
